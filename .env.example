# Optional: raises GitHub API rate limits for public repository inspection.
GITHUB_TOKEN=

# Optional: enables model-backed analysis in addition to deterministic fallback.
OPENAI_API_KEY=

# Optional: overrides the default OpenAI model used by src/lib/openai-analyzer.ts.
OPENMAINTAINER_MODEL=gpt-5.4-mini
